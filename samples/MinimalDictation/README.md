# MinimalDictation

This package-consumer smoke sample deliberately uses `PackageReference`, not
`ProjectReference`. Run `eng/PackLibraries.ps1` to create local packages, restore
the sample from that folder, build it, and execute it without Whisper initialization.
